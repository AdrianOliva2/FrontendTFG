import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dam.adrianoliva.proyecto.tfg',
  appName: 'FrontendTFG',
  webDir: 'dist/frontend-tfg',
  bundledWebRuntime: false
};

export default config;
